dependencies {
    compile 'io.netty:netty-handler:4.1.0.Final'
    compile 'io.netty:netty-codec-http:4.1.0.Final'
    compile 'io.netty:netty-transport-udt:4.1.0.Final'
    testCompile project(':reactivesocket-test')
}

task echoServer(type: JavaExec) {
    classpath = sourceSets.examples.runtimeClasspath
    main = 'io.reactivesocket.netty.EchoServer'
}
